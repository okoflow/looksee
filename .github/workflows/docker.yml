# TODO: not implemented yet. Manual trigger only, so nothing runs and
# GitHub does not flag the file as an invalid workflow.
name: Docker

on:
  workflow_dispatch:

jobs:
  todo:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "TODO"
